services:
  - type: web
    name: ai-stock-predictor-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt --no-cache-dir
    startCommand: python api.py
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.7
